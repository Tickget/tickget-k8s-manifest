apiVersion: v1
kind: ConfigMap
metadata:
  name: ticketing-server-config
data: {}
  # application.yml에서 사용할 환경 변수들
  # SPRING_DATASOURCE_URL: "jdbc:mysql://<DB_HOST>:<DB_PORT>/<DB_NAME>?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul"
  # SPRING_DATASOURCE_USERNAME: "<DB_USERNAME>"
  # 레디스 서비스 이름을 사용 (쿠버네티스 내부 DNS)
  # SPRING_DATA_REDIS_HOST: "redis-service" 
  # SPRING_DATA_REDIS_PORT: "6379"