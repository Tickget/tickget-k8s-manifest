apiVersion: v1
kind: Secret
metadata:
  name: ticketing-server-secret
type: Opaque
data:
  # MySQL Credentials
  # SPRING_DATASOURCE_USERNAME: S13P31A209
  SPRING_DATASOURCE_USERNAME: UzEzUDMxQTIwOQ==
  # SPRING_DATASOURCE_PASSWORD: tickget209
  SPRING_DATASOURCE_PASSWORD: dGlja2dldDIwOQ==

  # Redis Password (empty - no auth required)
  SPRING_DATA_REDIS_PASSWORD: ""

  # MongoDB credentials are embedded in URI in ConfigMap
  # No separate secret needed for MongoDB
