# apps/test-server/overlays/test/middleware.yaml
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: test-server-middleware
  namespace: test # ★ 이 미들웨어를 'test' 네임스페이스에 생성
spec:
  stripPrefix:
    prefixes:
      - /api/v1/test/tst # ★ 'test' 환경의 경로 (예시)