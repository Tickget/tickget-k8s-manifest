apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: strip-ticketing-prefix
  # Ingress와 동일한 네임스페이스에 생성 (아마도 'default')
spec:
  stripPrefix:
    prefixes:
      - /api/ticketing